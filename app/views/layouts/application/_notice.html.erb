<% flash.each do |k, v| %>
  <div class="alert alert-<%= k %> alert-dismissible fade show">
    <button type="button" class="btn-close" data-dismiss="alert" aria-label="Close"></button>

    <%= sanitize v.html_safe %>
  </div>
<% end %>
